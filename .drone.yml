---
kind: pipeline
name: default

steps:
  - name: build
    image: golang
    commands:
      - make build
      - make test

  - name: docker
    image: plugins/docker
    settings:
      repo: "localhost:5000/hello-app"
